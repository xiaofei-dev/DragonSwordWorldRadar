diff --git a/build/Verify-Source.ps1 b/build/Verify-Source.ps1
index ef07af5..d1c6df3 100644
--- a/build/Verify-Source.ps1
+++ b/build/Verify-Source.ps1
@@ -131,6 +131,8 @@ foreach ($marker in @(
     'RadarIdleTimerIntervalMs = 75',
     'DisabledTimerIntervalMs = 125',
     'MotionVisualSnapshot',
+    'ModeTransitionDebounceMs = 220',
+    'EVENTRADAR_GAME_PID',
     'resetChanged')) {
     if ($radar -notmatch [regex]::Escape($marker)) {
         throw "Performance/CodeDOM marker is missing: $marker"
@@ -142,7 +144,7 @@ if ($installCmd -notmatch '(?i)powershell\.exe' -or $installCmd -match '(?i)\bpw
     throw 'Install.cmd must use Windows PowerShell and must not use PowerShell Core.'
 }
 $install = Get-Content -LiteralPath (Join-Path $root 'src\installer\Install.ps1') -Raw
-foreach ($marker in @('OVERLAY_COMPILE_OK','INSTALLER_COMPILE_OK','WATCHER_READY','INSTALL_COMPLETE')) {
+foreach ($marker in @('OVERLAY_COMPILE_OK','INSTALLER_COMPILE_OK','SESSION_WATCHER_READY','INSTALL_COMPLETE')) {
     if ($install -notmatch [regex]::Escape($marker)) {
         throw "Installer marker is missing: $marker"
     }
diff --git a/src/host/DragonSwordWorldRadar.Host.ps1 b/src/host/DragonSwordWorldRadar.Host.ps1
index 4123173..bda5a68 100644
--- a/src/host/DragonSwordWorldRadar.Host.ps1
+++ b/src/host/DragonSwordWorldRadar.Host.ps1
@@ -74,6 +74,14 @@ namespace DragonSwordWorldRadar {
     Log ("FATAL " + ($_ | Out-String))
     throw
 } finally {
+    try {
+        Remove-Item -LiteralPath (Join-Path $runtime 'active-version.txt') -Force -ErrorAction SilentlyContinue
+        Remove-Item -LiteralPath (Join-Path $runtime 'launch.request') -Force -ErrorAction SilentlyContinue
+        Get-ChildItem -LiteralPath (Join-Path $runtime 'bridge') -File -ErrorAction SilentlyContinue |
+            Where-Object { $_.Name -like 'radar_state_*.json' -or $_.Name -like 'radar_motion_*.dat' } |
+            Remove-Item -Force -ErrorAction SilentlyContinue
+        Log 'SESSION_CLEANUP_COMPLETE'
+    } catch {}
     if ($ownsMutex -and $mutex -ne $null) {
         try { $mutex.ReleaseMutex() } catch {}
         try { $mutex.Dispose() } catch {}
diff --git a/src/host/DragonSwordWorldRadar.Watcher.ps1 b/src/host/DragonSwordWorldRadar.Watcher.ps1
index cc6f007..253dc2f 100644
--- a/src/host/DragonSwordWorldRadar.Watcher.ps1
+++ b/src/host/DragonSwordWorldRadar.Watcher.ps1
@@ -43,6 +43,15 @@ try {
         exit 2
     }
     Remove-Item -LiteralPath (Join-Path $runtime 'reinstall-required.json') -Force -ErrorAction SilentlyContinue
+    $gameProcess = Get-Process -Name 'DSClient-Win64-Shipping' -ErrorAction SilentlyContinue |
+        Sort-Object StartTime -Descending |
+        Select-Object -First 1
+    if (-not $gameProcess) {
+        Log "GAME_SESSION_NOT_FOUND stamp=$RequestStamp"
+        exit 0
+    }
+    $env:EVENTRADAR_GAME_PID = [string]$gameProcess.Id
+    Log ("GAME_SESSION_BOUND stamp={0} gamePid={1}" -f $RequestStamp,$gameProcess.Id)
     & (Join-Path $PSScriptRoot 'DragonSwordWorldRadar.Host.ps1') -ModDir $ModDir
     Log "HOST_RETURN stamp=$RequestStamp pid=$PID"
 } catch {
diff --git a/src/host/DragonSwordWorldRadar.Watcher.vbs b/src/host/DragonSwordWorldRadar.Watcher.vbs
index 8be422f..f9c9212 100644
--- a/src/host/DragonSwordWorldRadar.Watcher.vbs
+++ b/src/host/DragonSwordWorldRadar.Watcher.vbs
@@ -61,6 +61,7 @@ Do
             LogLine "OVERLAY_PROCESS_START stamp=" & currentStamp
             rc = shell.Run(command, 0, True)
             LogLine "OVERLAY_PROCESS_EXIT stamp=" & currentStamp & " rc=" & CStr(rc)
+            Exit Do
         End If
     End If
     WScript.Sleep 250
diff --git a/src/installer/Install.ps1 b/src/installer/Install.ps1
index 6bc0299..111ff92 100644
--- a/src/installer/Install.ps1
+++ b/src/installer/Install.ps1
@@ -412,32 +412,8 @@ try {
     foreach ($oldShortcut in @('EventRadarWatcher.lnk','EventRadar.lnk','DragonSwordWorldRadarWatcher.lnk','DragonSwordWorldRadar.lnk')) {
         Remove-Item -LiteralPath (Join-Path $startupDirectory $oldShortcut) -Force -ErrorAction SilentlyContinue
     }
-    $shortcutPath = Join-Path $startupDirectory 'DragonSwordWorldRadar.lnk'
-    $shell = New-Object -ComObject WScript.Shell
-    $shortcut = $shell.CreateShortcut($shortcutPath)
-    $shortcut.TargetPath = "$env:SystemRoot\System32\wscript.exe"
-    $watcherArguments = '//B //NoLogo "' + $watcherScript.Replace('"','""') + '" "' + $modRoot.Replace('"','""') + '"'
-    $shortcut.Arguments = $watcherArguments
-    $processWorkDir = Join-Path ([IO.Path]::GetTempPath()) 'DragonSwordWorldRadar'
-    New-Item -ItemType Directory -Force -Path $processWorkDir | Out-Null
-    $shortcut.WorkingDirectory = $processWorkDir
-    $shortcut.WindowStyle = 7
-    $shortcut.Save()
-
-    $watcherLog = Join-Path $logDir 'DragonSwordWorldRadar.Watcher.log'
-    Remove-Item -LiteralPath $watcherLog -Force -ErrorAction SilentlyContinue
-    $watcherProcess = Start-Process -FilePath $shortcut.TargetPath -ArgumentList $watcherArguments -WindowStyle Hidden -PassThru
-    $ready = $false
-    for ($attempt = 0; $attempt -lt 40; $attempt++) {
-        Start-Sleep -Milliseconds 100
-        if (Test-Path -LiteralPath $watcherLog) {
-            $text = [IO.File]::ReadAllText($watcherLog)
-            if ($text -match 'WATCHER_START') { $ready = $true; break }
-        }
-        if ($watcherProcess.HasExited) { break }
-    }
-    if (-not $ready) { throw 'The hidden DragonSwordWorldRadar WScript watcher did not become ready. Check runtime\logs\DragonSwordWorldRadar.Watcher.log.' }
-    Log 'WATCHER_READY host=wscript powershell_lifetime=game-only'
+    Stop-ExistingWatcher
+    Log 'SESSION_WATCHER_READY mode=ue4ss-launched lifetime=game-process'
 
     $allExecutables = @(Get-ChildItem -LiteralPath $modRoot -Filter '*.exe' -File -Recurse -ErrorAction SilentlyContinue)
     $unexpectedExecutables = @($allExecutables | Where-Object { [IO.Path]::GetFullPath($_.FullName) -ne [IO.Path]::GetFullPath($bundledOozPath) })
@@ -446,7 +422,7 @@ try {
     }
     if ($allExecutables.Count -ne 1) { throw "Expected exactly one bundled tool executable, found $($allExecutables.Count)." }
 
-    Log ("INSTALL_COMPLETE version={0} customExe=0 bundledToolExe=1 watcher=wscript transientPowerShell=true" -f $releaseVersion)
+    Log ("INSTALL_COMPLETE version={0} customExe=0 bundledToolExe=1 watcher=per-session-wscript transientPowerShell=true" -f $releaseVersion)
     Write-Host ("DragonSwordWorldRadar {0} installed. Generated {1} treasure records and {2} world-boss records for game version {3}." -f $releaseVersion,$recordCount,$bossRecordCount,$identity.display_version)
     Write-Host 'Start the game normally. F7 enables the radar; F8 disables it.'
 } catch {
diff --git a/src/overlay/Platform/GameProcessFinder.cs b/src/overlay/Platform/GameProcessFinder.cs
index 65c7291..db0e048 100644
--- a/src/overlay/Platform/GameProcessFinder.cs
+++ b/src/overlay/Platform/GameProcessFinder.cs
@@ -8,6 +8,38 @@ namespace DragonSwordWorldRadar
         private const string ProcessName =
             "DSClient-Win64-Shipping";
 
+        public static Process OpenExact(int processId)
+        {
+            if (processId <= 0)
+            {
+                return null;
+            }
+
+            Process process = null;
+            try
+            {
+                process = Process.GetProcessById(processId);
+                if (!process.HasExited
+                    && String.Equals(
+                        process.ProcessName,
+                        ProcessName,
+                        StringComparison.OrdinalIgnoreCase))
+                {
+                    return process;
+                }
+            }
+            catch
+            {
+                // The exact game process may have exited between checks.
+            }
+
+            if (process != null)
+            {
+                process.Dispose();
+            }
+            return null;
+        }
+
         public static Process OpenTracked(int processId)
         {
             if (processId > 0)
diff --git a/src/overlay/UI/RadarForm.cs b/src/overlay/UI/RadarForm.cs
index 1ac65d7..8249dac 100644
--- a/src/overlay/UI/RadarForm.cs
+++ b/src/overlay/UI/RadarForm.cs
@@ -30,6 +30,7 @@ namespace DragonSwordWorldRadar
         private const int GameLifetimeCheckIntervalMs = 1000;
         private const int MotionActivityWindowMs = 300;
         private const int MotionStaleTimeoutMs = 2500;
+        private const int ModeTransitionDebounceMs = 220;
 
         // Height indicators use a continuous 180-degree scale. The player
         // actor origin sits above the treasure reference point, so the
@@ -82,6 +83,10 @@ namespace DragonSwordWorldRadar
         private bool _hasSeenGameProcess;
         private DateTime _gameProcessMissingSinceUtc;
         private DateTime _nextGameLifetimeCheckUtc;
+        private string _presentedMode = "disabled";
+        private string _pendingMode;
+        private DateTime _pendingModeSinceUtc;
+        private bool _transitionHidden = true;
 
         public RadarForm()
         {
@@ -142,6 +147,24 @@ namespace DragonSwordWorldRadar
                 Visible = true
             };
 
+            int configuredGameProcessId;
+            if (Int32.TryParse(
+                    Environment.GetEnvironmentVariable(
+                        "EVENTRADAR_GAME_PID"),
+                    NumberStyles.Integer,
+                    CultureInfo.InvariantCulture,
+                    out configuredGameProcessId)
+                && configuredGameProcessId > 0)
+            {
+                _gameProcessId = configuredGameProcessId;
+                _hasSeenGameProcess = true;
+            }
+
+            Shown += delegate
+            {
+                HideForModeTransition("startup");
+            };
+
             _timer = new Timer
             {
                 Interval = 100
@@ -186,6 +209,7 @@ namespace DragonSwordWorldRadar
             _worldTreasureRenderBuffer.Dispose();
             _bossMarkerRenderer.Dispose();
             _heightIndicatorRenderer.Dispose();
+            ClearSessionBridgeFiles();
             if (_highResolutionTimerEnabled)
             {
                 try
@@ -208,6 +232,11 @@ namespace DragonSwordWorldRadar
             bool paintFailed = false;
             try
             {
+                if (_transitionHidden)
+                {
+                    return;
+                }
+
                 RadarState state = _state;
                 MotionFrame motion = GetCompatibleMotion(state);
                 if (state == null
@@ -221,9 +250,7 @@ namespace DragonSwordWorldRadar
                     SmoothingMode.AntiAlias;
                 bool showDebugCoordinates = DebugSettings.Enabled;
                 float textScale = NormalizeTextScale(state.textScale);
-                string mode = motion == null
-                    ? state.mode
-                    : motion.Mode;
+                string mode = _presentedMode;
                 double playerX = motion == null
                     ? state.playerX
                     : motion.PlayerX;
@@ -1291,7 +1318,7 @@ namespace DragonSwordWorldRadar
 
         private void UpdateTimerInterval(DateTime now)
         {
-            string mode = GetEffectiveMode();
+            string mode = _presentedMode;
             DateTime activityUtc = _lastMotionVisualChangeUtc;
             if (_lastModeTransitionUtc > activityUtc)
             {
@@ -1336,7 +1363,6 @@ namespace DragonSwordWorldRadar
             DateTime now = DateTime.UtcNow;
             bool redraw = false;
             bool geometryChanged = false;
-            string previousMode = GetEffectiveMode();
 
             if (now >= _nextStaticStatePollUtc)
             {
@@ -1483,11 +1509,10 @@ namespace DragonSwordWorldRadar
                 }
             }
 
-            string currentMode = GetEffectiveMode();
-            geometryChanged = !String.Equals(
-                previousMode,
-                currentMode,
-                StringComparison.Ordinal);
+            string rawMode = GetEffectiveMode();
+            geometryChanged = AdvancePresentedMode(
+                rawMode,
+                now);
             if (geometryChanged)
             {
                 _lastModeTransitionUtc = now;
@@ -1499,8 +1524,12 @@ namespace DragonSwordWorldRadar
                     GeometryCheckIntervalMs);
                 MoveOverGameWindow();
             }
+            if (geometryChanged)
+            {
+                CompleteModeTransition();
+            }
             UpdateTimerInterval(now);
-            if (redraw || geometryChanged)
+            if (!_transitionHidden && (redraw || geometryChanged))
             {
                 _performance.RecordInvalidate();
                 Invalidate();
@@ -2015,6 +2044,189 @@ namespace DragonSwordWorldRadar
                 <= 250.0 * 250.0;
         }
 
+        private bool AdvancePresentedMode(
+            string rawMode,
+            DateTime now)
+        {
+            string normalizedMode = String.IsNullOrEmpty(rawMode)
+                ? "disabled"
+                : rawMode;
+            if (String.Equals(
+                    normalizedMode,
+                    _presentedMode,
+                    StringComparison.Ordinal))
+            {
+                bool transitionCancelled =
+                    _pendingMode != null && _transitionHidden;
+                _pendingMode = null;
+                _pendingModeSinceUtc = DateTime.MinValue;
+                return transitionCancelled;
+            }
+
+            if (!String.Equals(
+                    normalizedMode,
+                    _pendingMode,
+                    StringComparison.Ordinal))
+            {
+                _pendingMode = normalizedMode;
+                _pendingModeSinceUtc = now;
+                HideForModeTransition(
+                    "candidate=" + normalizedMode);
+                return false;
+            }
+
+            if (now - _pendingModeSinceUtc
+                < TimeSpan.FromMilliseconds(
+                    ModeTransitionDebounceMs))
+            {
+                return false;
+            }
+            if (String.Equals(
+                    normalizedMode,
+                    "world",
+                    StringComparison.Ordinal)
+                && !HasValidWorldMapTransform())
+            {
+                return false;
+            }
+
+            string previousMode = _presentedMode;
+            _presentedMode = normalizedMode;
+            _pendingMode = null;
+            _pendingModeSinceUtc = DateTime.MinValue;
+            ErrorLog.WriteDebug(
+                "Overlay mode prepared: " +
+                previousMode + " -> " + _presentedMode);
+            return true;
+        }
+
+        private bool HasValidWorldMapTransform()
+        {
+            RadarState state = _state;
+            MotionFrame motion = GetCompatibleMotion(state);
+            WorldMapState map = motion == null
+                ? (state == null ? null : state.worldMap)
+                : motion.WorldMap;
+            return map != null
+                && IsFinitePositive(map.dimensions)
+                && IsFinitePositive(map.uiSize)
+                && IsFinitePositive(map.zoom)
+                && IsFinitePositive(map.viewportWidth)
+                && IsFinitePositive(map.viewportHeight)
+                && IsFinitePositive(map.viewportScale)
+                && IsFinite(map.left)
+                && IsFinite(map.top)
+                && IsFinite(map.playerWorldX)
+                && IsFinite(map.playerWorldY)
+                && IsFinite(map.playerMapX)
+                && IsFinite(map.playerMapY);
+        }
+
+        private static bool IsFinite(double value)
+        {
+            return !Double.IsNaN(value)
+                && !Double.IsInfinity(value);
+        }
+
+        private static bool IsFinitePositive(double value)
+        {
+            return IsFinite(value) && value > 0.0;
+        }
+
+        private void HideForModeTransition(string reason)
+        {
+            bool wasHidden = _transitionHidden;
+            _transitionHidden = true;
+            if (IsHandleCreated)
+            {
+                NativeMethods.ShowWindow(
+                    Handle,
+                    NativeMethods.SwHide);
+            }
+            if (!wasHidden)
+            {
+                ErrorLog.WriteDebug(
+                    "Overlay hidden for mode transition: " + reason);
+            }
+        }
+
+        private void CompleteModeTransition()
+        {
+            if (String.Equals(
+                    _presentedMode,
+                    "disabled",
+                    StringComparison.Ordinal))
+            {
+                _transitionHidden = true;
+                if (IsHandleCreated)
+                {
+                    NativeMethods.ShowWindow(
+                        Handle,
+                        NativeMethods.SwHide);
+                }
+                return;
+            }
+
+            _transitionHidden = false;
+            if (IsHandleCreated)
+            {
+                NativeMethods.ShowWindow(
+                    Handle,
+                    NativeMethods.SwShowNoActivate);
+            }
+            _performance.RecordInvalidate();
+            Invalidate();
+            ErrorLog.WriteDebug(
+                "Overlay shown after mode transition: mode=" +
+                _presentedMode);
+        }
+
+        private static void ClearSessionBridgeFiles()
+        {
+            try
+            {
+                string bridgeDirectory = Path.Combine(
+                    ModPath.BaseDirectory,
+                    "runtime",
+                    "bridge");
+                if (!Directory.Exists(bridgeDirectory))
+                {
+                    return;
+                }
+                string[] patterns =
+                {
+                    "radar_state_*.json",
+                    "radar_motion_*.dat"
+                };
+                for (int patternIndex = 0;
+                    patternIndex < patterns.Length;
+                    patternIndex++)
+                {
+                    string[] files = Directory.GetFiles(
+                        bridgeDirectory,
+                        patterns[patternIndex],
+                        SearchOption.TopDirectoryOnly);
+                    for (int fileIndex = 0;
+                        fileIndex < files.Length;
+                        fileIndex++)
+                    {
+                        try
+                        {
+                            File.Delete(files[fileIndex]);
+                        }
+                        catch
+                        {
+                            // Session cleanup is best effort.
+                        }
+                    }
+                }
+            }
+            catch
+            {
+                // Form shutdown must not fail because stale bridge cleanup failed.
+            }
+        }
+
         private void CheckGameLifetime()
         {
             DateTime now = DateTime.UtcNow;
@@ -2029,7 +2241,9 @@ namespace DragonSwordWorldRadar
             try
             {
                 using (Process process =
-                    GameProcessFinder.OpenTracked(_gameProcessId))
+                    _hasSeenGameProcess
+                        ? GameProcessFinder.OpenExact(_gameProcessId)
+                        : GameProcessFinder.OpenTracked(_gameProcessId))
                 {
                     gamePresent = process != null;
                     if (gamePresent)
@@ -2186,7 +2400,7 @@ namespace DragonSwordWorldRadar
         private bool IsWorldMapMode()
         {
             return String.Equals(
-                GetEffectiveMode(),
+                _presentedMode,
                 "world",
                 StringComparison.Ordinal);
         }
diff --git a/src/ue4ss/main.lua b/src/ue4ss/main.lua
index 13231a8..3dd62c6 100644
--- a/src/ue4ss/main.lua
+++ b/src/ue4ss/main.lua
@@ -327,7 +327,21 @@ local function start_overlay()
     end
     request:write(tostring(os.time()), "\n")
     request:close()
-    log("PowerShell overlay launch requested through hidden watcher: " .. request_path)
+
+    local watcher_path = directory
+        .. "\\host\\DragonSwordWorldRadar.Watcher.vbs"
+    local command = 'cmd.exe /c start "" /b wscript.exe //B //NoLogo "'
+        .. watcher_path
+        .. '" "'
+        .. directory
+        .. '"'
+    local ok, result = pcall(os.execute, command)
+    if not ok then
+        log("Could not launch the per-session overlay watcher: "
+            .. tostring(result))
+        return
+    end
+    log("Per-session overlay watcher launched: " .. request_path)
 end
 
 local function resolve_state_paths()
