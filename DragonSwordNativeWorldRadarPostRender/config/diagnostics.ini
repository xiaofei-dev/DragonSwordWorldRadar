[diagnostics]
# Public releases keep diagnostics disabled. Set debug_logging=true only for a
# troubleshooting session, then restart the game. This file is read once when
# the native Mod starts; it is never polled while the game is running.
debug_logging=false
