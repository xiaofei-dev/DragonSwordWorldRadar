return {
    -- Overlay-only settings. Native sampling is configured in config.ini.
    debug_logging = true,
    diagnostic_logging = true,
    diagnostic_verbose = false,
    diagnostic_perf_interval_seconds = 5,
    high_resolution_timer = false,
    show_assaults = true,
}
