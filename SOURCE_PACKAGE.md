# Source package

This repository snapshot matches DragonSwordWorldRadar 0.3.2c.

- Runtime release files are assembled by `build/Build-Release.ps1`.
- `vendor/ooz/ooz.exe` is a bundled installation-time helper binary; verify its upstream redistribution terms before publishing a public release.
- Generated runtime data, logs, bridge files, and diagnostics archives are intentionally excluded from source control.
