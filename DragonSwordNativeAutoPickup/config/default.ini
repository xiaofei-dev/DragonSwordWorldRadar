[auto_pickup]
# F9 starts/stops a 60-second read-only DropItemActor closed-loop diagnostic.
# It discovers and traces one nearby ordinary drop but never invokes pickup.
enabled_on_launch=false
read_only_diagnostic=true
toggle_hotkey=F9
radius_meters=4.5
max_queue=128
perf_log_interval_seconds=30
